<Window x:Class="InventoryApp.View.ProductModal"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				xmlns:viewmodel="clr-namespace:InventoryApp.ViewModel"
				d:DataContext="{d:DesignInstance Type=viewmodel:ProductViewModel}"
		mc:Ignorable="d"
        Title="ProductModal" Height="450" Width="400">
	<Window.Resources>
		
		<Style x:Key="ErrorText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="Red"/>
			<Setter Property="FontSize" Value="11"/>
			<Setter Property="Margin" Value="0,2,0,0"/>
			<Setter Property="FontWeight" Value="Normal"/>
		</Style>
	</Window.Resources>
	<Grid RowDefinitions="9*,1*">
		<StackPanel Margin="10,20,10,20">
			<TextBlock Text="Name" HorizontalAlignment="Center" />
			<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="100,5,100,0" />
			<TextBlock Margin="100,5,100,20" HorizontalAlignment="Center" Text="{Binding NameError}"  Style="{StaticResource ErrorText}" Visibility="{Binding NameError}"/>

			<TextBlock Text="Price" HorizontalAlignment="Center" />
			<TextBox Text="{Binding Price}" Margin="100,5,100,20" />
			<TextBlock HorizontalAlignment="Center" Text="{Binding PriceError}" Style="{StaticResource ErrorText}" Visibility="{Binding PriceError}"/>

			<TextBlock Text="Quantity" HorizontalAlignment="Center" />
			<TextBox Text="{Binding Quantity}" Margin="100,5,100,20" />
			<TextBlock HorizontalAlignment="Center" Text="{Binding QuantityError}" Style="{StaticResource ErrorText}" Visibility="{Binding QuantityError}"/>

			<TextBlock Text="Category" HorizontalAlignment="Center" />
			<ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCategory}" Margin="100,5,100,20" />
			<TextBlock HorizontalAlignment="Center" Text="{Binding CategoryError}" Style="{StaticResource ErrorText}" Visibility="{Binding CategoryError}"/>

		</StackPanel>
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" HorizontalAlignment="Center" >
			<Button 
			Margin="5,0,5,0" 
			Padding="5,0,5,0" 
			Content="Save"  
			IsDefault="True"
			IsEnabled="{Binding IsValid}"
			Click="SaveButton_Click"
			/>
			<Button Margin="5,0,5,0" 
			Padding="5,0,5,0" 
			Content="Cancel" 
			IsCancel="True" 
			 />
		</StackPanel>
	</Grid>
</Window>
