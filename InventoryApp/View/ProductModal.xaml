<Window x:Class="InventoryApp.View.ProductModal"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				xmlns:viewmodel="clr-namespace:InventoryApp.ViewModel"
				WindowStartupLocation="CenterOwner"
				d:DataContext="{d:DesignInstance Type=viewmodel:ProductViewModel}"
				mc:Ignorable="d"
        Title="ProductModal" Height="450" Width="400">

	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Themes/ValidationTemplates.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>
	<Grid RowDefinitions="9*,1*">
		<StackPanel Margin="20,20,20,0">
			<TextBlock Text="Name" HorizontalAlignment="Center" />
			<TextBox Margin="0,0,0,22" Text="{Binding Name, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Validation.ErrorTemplate="{StaticResource errorTemplate}"/>

			<TextBlock Text="Price" HorizontalAlignment="Center" />
			<TextBox Margin="0,0,0,22" Text="{Binding Price, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Validation.ErrorTemplate="{StaticResource errorTemplate}"/>


			<TextBlock Text="Quantity" HorizontalAlignment="Center" />
			<TextBox Margin="0,0,0,22" Text="{Binding Quantity, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Validation.ErrorTemplate="{StaticResource errorTemplate}"/>


			<TextBlock Text="Category" HorizontalAlignment="Center" />
			<ComboBox 
			ItemsSource="{Binding Categories}" 
			DisplayMemberPath="Name"
			SelectedItem="{Binding CategoryId, ValidatesOnDataErrors=True}"
			
			Validation.ErrorTemplate="{StaticResource errorTemplate}"
			Margin="30,5,30,20" />


		</StackPanel>
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" HorizontalAlignment="Center" >
			<Button 
			Margin="5,0,5,0" 
			Padding="5,0,5,0" 
			Content="Save"  
			IsDefault="True"
			IsEnabled="{Binding IsValid}"
			Click="SaveButton_Click"
			/>
			<Button Margin="5,0,5,0" 
			Padding="5,0,5,0" 
			Content="Cancel" 
			IsCancel="True" 
			 />
		</StackPanel>
	</Grid>
</Window>
